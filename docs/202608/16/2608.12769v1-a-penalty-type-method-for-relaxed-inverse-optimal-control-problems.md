---
title: A penalty-type method for relaxed inverse optimal control problems
title_zh: 一种用于松弛逆最优控制问题的惩罚型方法
authors: "Floriane Mefo Kue, Patrick Mehlitz, Thorsten Raasch"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.12769v1"
tags: ["query:inverse-rl"]
score: 9.0
evidence: 松弛逆最优控制的惩罚型数值方法
tldr: 针对一类由逆最优控制产生的双层优化问题，提出了一种罚型方法。该方法通过惩罚松弛值函数约束构建代理问题，并在迭代中解耦上下层决策变量，仅在可行性度量未充分改善时才增大惩罚参数，从而避免线搜索且每步只需求解一次下层问题。在合理假设下获得子序列收敛性，数值实验结合松弛参数趋于零验证了方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.12769v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 633, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.12769v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1516, \"height\": 931, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.12769v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1514, \"height\": 936, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.12769v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1511, \"height\": 247, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.12769v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1314, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.12769v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1336, \"height\": 511, \"label\": \"Table\"}]"
motivation: 逆最优控制问题常转化为带值函数约束的双层优化，其数值求解困难，现有方法依赖强假设或复杂线搜索。
method: 提出罚型方法，将松弛值函数约束罚入目标，上下层变量解耦更新，惩罚参数按可行性改善情况自适应调整。
result: 无需线搜索和惩罚参数趋于无穷，每迭代仅一次下层求解，在温和假设下证明子序列收敛到驻点。
conclusion: 数值实验表明，松弛参数同步趋零时方法高效稳定，为逆最优控制提供实用求解框架。
---

## 摘要
本文致力于引入和分析一种惩罚型方法，用于数值处理一类由逆最优控制产生的双层优化问题。该算法旨在计算相关松弛值函数重构的稳定点。这是通过确定与一系列替代问题相关的稳定点序列来实现的，其中松弛值函数约束被惩罚，上层和下层决策变量的更新被解耦，并且仅在那些未伴随可行性度量充分改善的迭代中增大惩罚参数。所得到的方法不包含任何线搜索，下层问题每次迭代只需评估一次，并且惩罚参数不需要趋于无穷大。然而，在合理假设下获得了子序列收敛结果。数值实验（其中松弛参数也趋于零）展示了该方法的有效性。

## Abstract
This paper is devoted to the introduction and analysis   of a penalty-type method for the numerical treatment of   a class of bilevel optimization problems arising from inverse optimal control.   The algorithm is designed to compute stationary points of the associated relaxed value function reformulation.   This is achieved by determining a sequence of stationary points associated with a sequence of surrogate problems   where the relaxed value function constraint is penalized,   where the updates of upper- and lower-level decision variables are decoupled,   and where the penalty parameter is enlarged only in those iterations   which do not come along with a sufficient improvement of some feasibility measure.   The resulting method does not comprise any linesearch, the lower-level problem has to be evaluated just once per iteration,   and the penalty parameter does not need to be driven to infinity.   Nevertheless, subsequential convergence results are obtained under reasonable assumptions.   Numerical experiments, where the relaxation parameter is also driven to zero, visualize effectiveness of the approach.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：逆最优控制（Inverse Optimal Control, IOC）是一类特殊的双层优化问题，其目标是通过观测数据重建下层最优控制问题中的未知参数。该问题在人类运动建模、参数追踪、多智能体调度、气体输送等领域有重要应用。由于双层优化问题本身缺乏稳定性，单层重构往往违反约束规范，导致数值求解困难。
- **核心问题**：针对文献中已有的松弛值函数重构（relaxed value function reformulation）形式，即问题 (IOC_ε)，如何设计一种**计算代价低、无需线搜索、不要求惩罚参数趋于无穷**的数值算法，以高效计算其稳定点，并在松弛参数 ε 趋零时逼近原 IOC 问题的解。
- **整体含义**：论文提出了一种简单的惩罚型方法，将松弛值函数约束罚入目标函数，通过解耦上下层变量更新和自适应惩罚参数调整，在温和假设下获得子序列收敛性，为解决逆最优控制问题提供了一种实用且低成本的求解框架。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：
- 将带松弛值函数约束的 (IOC_ε) 问题转化为无约束（仅含集合约束）的惩罚问题 (IOC_ε(α))，利用非光滑精确惩罚函数的性质，避免直接处理隐含且昂贵的下层最优值函数 φ 及其导数。
- 通过求解一系列参数化惩罚问题的稳定点来逼近 (IOC_ε) 的稳定点，其中惩罚参数 α 仅在可行性度量未充分改善的迭代中增大。

**关键技术细节**：
- **惩罚问题形式**：
  \[
  \min_{x,y,u} F(x,y,u) + \alpha \max\{f(x,y,u)-\varphi(x)-\varepsilon, 0\}
  \]
  约束为 \(x\in X_{ad},\ Ay=Bu,\ u\in U_{ad}\)。
- **稳定性条件**：利用 Clarke 次微分和最大值函数的次微分规则，推导出惩罚问题的 KKT 型稳定系统（式 4.2），其中包含一个标量 t ∈ ∂max{·,0}(θ)，用于处理罚项的不可微性。
- **精确惩罚性质**：若 (x̄,ȳ,ū) 是 (IOC_ε) 的稳定点且对应乘子 λ_vf，则对任意 α ≥ λ_vf，该点也是惩罚问题的稳定点（命题 4.2），从而说明无需将 α 推向无穷大。
- **参数化惩罚子问题 (PP_α(x))**：固定上层变量 x 后，对下层变量 (y,u) 求解一个凸优化问题，其最优解和相应乘子唯一存在且关于 x Lipschitz 连续（命题 4.4）。该子问题等价于一个方形混合非线性互补系统。
- **上层变量更新**：利用投影步 \(x_{k+1} = \text{proj}_{X_{ad}}(x_k - \eta w_k)\) 更新 x，其中 \(w_k\) 包含惩罚梯度和下层最优值函数导数信息。
- **算法流程（Algorithm 4.6）**：
  1. 给定初值 \(x_0\in X_{ad}\)，惩罚参数 α₀，参数 γ>1, η>0, κ∈(0,1)。
  2. 每次迭代：给定 x_k 和 α_k，求解系统（4.9）得到 (y_k, u_k, λ_p^k, λ_u^k)，即 (PP_{α_k}(x_k)) 的稳定点。
  3. 计算下层最优值函数 φ(x_k)，并计算可行性度量 V_k = f(x_k,y_k,u_k)−φ(x_k)−ε。
  4. 若 V_k ≤ 0，则返回当前点（可行且近似稳定）。
  5. 否则，若 V_k > κ V_{k−1}，则 α_{k+1} = γ α_k；否则 α_{k+1} = α_k。
  6. 计算 w_k 并更新 x_{k+1} = proj_{X_ad}(x_k − η w_k)。
- **收敛性分析**：
  - 引理 4.9：在 Assumption 4.7 下，惩罚参数序列 {α_k} 有界（否则将矛盾于 V_k 的正性和 ε>0）。
  - 引理 4.10：当 η 足够小时，x_{k+1}−x_k → 0，证明基于 ϕ_ᾱ 的 Lipschitz 梯度和类梯度下降的不等式。
  - 定理 4.11：序列 { (x_k,y_k,u_k) } 有弱收敛子列；每个弱聚点实际上强收敛，并且是 (IOC_ε) 的稳定点（满足 KKT 条件 4.1）。

## 3. 实验设计：使用了哪些数据集 / 场景，benchmark 是什么，对比了哪些方法

- **问题实例**：采用文献 [6] 中已有的逆最优控制测试问题（2维参数空间，n=2）。
  - 域 Ω = (−1,1)×(1,1)，Y = H₀¹(Ω)，U = V = L²(Ω)。
  - 下层状态方程为泊松方程 Ay = −Δy = Bu（B 为 L²(Ω) 到 H⁻¹(Ω) 的自然嵌入）。
  - 控制约束为逐点上下界：U_ad = {u ∈ L²(Ω) | u_a ≤ u ≤ u_b a.e.}。
  - 上层目标 F(x,y,u) = ½‖y−y_o‖²_{L²} + ⟨(0.1, 0.3), x⟩，观测状态 y_o 为两个高斯峰的组合。
  - 参数集 X_ad 为三角形 conv{(0,0),(1,0),(0,1)}。
- **离散化**：采用线性有限元（441 个顶点，800 个三角单元，网格尺寸 h=0.1），对控制变量使用质量集中（mass lumping）以保证互补条件的离散传递。具体求解系统（5.3）。
- **实验场景**：
  - 固定 σ = 10⁻²，变化松弛参数 ε 从 10⁻¹ 到 10⁻⁸，共 8 组实验。
  - 另有一组 σ = 10⁻⁴、ε = 10⁻⁸ 的额外实验，展示不同正则化强度的影响。
- **Benchmark**：没有与其他数值方法进行直接对比，但引用了文献 [6] 中相同网格设置下的全局解结果（x* ≈ (0.3, 0.3)，全局最优值 ≈ 0.6347）作为参照。
- **方法对比**：文中明确说明其方法与 [6] 的全局解方法不同，后者计算全局极小点但代价高昂，而本文方法仅计算稳定点，单次迭代成本低。

## 4. 资源与算力

- 论文**未提供 GPU 型号、数量或训练时长**等深度学习常用算力信息。
- 实验在普通台式机上进行：Intel Core i5-9500T（2.20 GHz），16 GB RAM，Windows 11 Pro（64-bit），MATLAB R2025b。
- 总体计算时间约 10 秒至 22 秒（8 组实验），每迭代平均约 0.25–0.26 秒，属于轻量级数值实验。

## 5. 实验数量与充分性

- **实验数量**：主实验共 8 组（ε 变化），另加 1 组 σ 变化的补充实验，总计 9 组数值实验。所有实验均在同一个测试问题上进行，没有多个数据集或消融实验。
- **充分性与公平性**：
  - 覆盖了松弛参数 ε 从大到小的完整范围，验证了极限行为。
  - 提供了详细的迭代次数、计算时间、跟踪误差和收敛性指标。
  - 未与其他已发表方法（如 [6] 的全局解法）进行直接数值对比，缺乏并排比较实验。
  - 没有针对算法中的超参数（如 α₀、γ、η、κ）进行敏感性分析。
  - 只测试了单一问题实例，缺乏更广泛问题类别上的验证。

## 6. 论文的主要结论与发现

- 算法能够在不需要线搜索、不需要将惩罚参数趋于无穷的情况下，产生 (IOC_ε) 的稳定点序列，并在温和假设下获得子序列收敛到 (IOC_ε) 的稳定点。
- 惩罚参数在迭代中自动保持有界，避免了经典罚方法中参数无限增大的问题。
- 数值结果显示，随着 ε 趋零，上层变量 x 逐渐逼近真实参数 (0.3, 0.3)，上层目标值趋于全局最优值（约 0.6347），可行性违规变小，验证了方法的有效性。
- 更小的 σ 允许状态更贴近观测状态，误差更小。
- 计算代价低：平均每迭代仅需约 0.26 秒，随 ε 减小迭代次数从 40 增至 85，总时间不超过约 22 秒。

## 7. 优点

- **计算效率高**：每次迭代只需一次下层问题求解，无任何线搜索，避免了反复调用隐含昂贵的 φ 函数。
- **理论性质良好**：在 Robinson 约束规范成立的前提下，利用非光滑精确惩罚的性质，证明了惩罚参数有界，无需趋于无穷。
- **实现简单**：算法结构清晰，投影步和混合互补系统求解均为标准操作，易于实现。
- **收敛性分析扎实**：包括 Lipschitz 连续解映射、最优值函数可微性证明、子序列收敛到稳定点等。
- **数值实验充分展示算法行为**：给出了不同 ε 下的详细结果，包括最优性间隙、可行性违规、迭代次数、时间、跟踪精度，有助于理解算法特性。

## 8. 不足与局限

- **仅考虑有限维上层变量**：论文分析不覆盖上层变量为无限维的情形，尽管正文提到可通过额外紧性假设扩展，但尚未给出。
- **实验覆盖范围有限**：仅基于一个二维参数测试问题，没有多数据集、多问题实例或更大规模测试，泛化性能未验证。
- **缺乏方法对比**：未与文献中其他逆最优控制求解方法（如 [6] 的全局方法或半光滑牛顿法）进行直接数值比较。
- **超参数敏感性与自适应策略**：η 的选择需要满足足够小的条件，但论文只给出理论存在性，未提供实际选择准则；也未对 α₀、γ、κ 等进行系统调参实验。
- **终止条件依赖于 V_k ≤ 0**：在实际数值中可能因离散误差而提前终止或无法严格满足，论文已指出 ε=10⁻⁸ 时最优性间隙出现轻微负值，属于数值容差问题，但未提出更稳健的终止判据。
- **松弛参数 ε 的更新策略未纳入算法**：论文仅将 ε 视为固定参数，实际应用需要外部驱动 ε 趋零，尚未设计内嵌的 ε 更新机制。

（完）
