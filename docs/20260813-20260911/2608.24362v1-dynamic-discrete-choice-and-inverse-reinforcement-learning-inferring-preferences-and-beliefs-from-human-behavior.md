---
title: "Dynamic Discrete Choice and Inverse Reinforcement Learning: Inferring Preferences and Beliefs From Human Behavior"
title_zh: 动态离散选择与逆强化学习：从人类行为中推断偏好与信念
authors: "Pranjal Rawat, John Rust"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.24362v1"
tags: ["query:inverse-rl"]
score: 9.0
evidence: 动态离散选择与逆强化学习的综述
tldr: 动态离散选择（DDC）与逆向强化学习（IRL）从计量经济学和机器学习两条路径解决从序贯行为推断偏好的同一问题。二者在MDP框架下数学形式趋同，IRL的软Q学习与DDC极值冲击模型均产生softmax选择概率和光滑贝尔曼方程。文章比较了DDC的极大似然、条件选择概率、策略迭代法与IRL的最大熵、对抗及无模型时序差分法，提出结合时序差分与计量两阶段的无模型估计是融合桥梁。识别问题与维度灾难是共同挑战，交叉融合有望推动方法进步。
source: arxiv
selection_source: fresh_fetch
motivation: DDC与IRL虽独立发展，却面对从序贯行为推断偏好的共同难题，二者数学形式已趋同但方法互有短长。
method: 系统比较DDC的极大似然、条件选择概率、策略迭代与IRL的最大熵、对抗、无模型时序差分估计。
result: 揭示IRL软Q学习与DDC极值冲击模型均导出softmax选择概率和光滑Bellman方程，无模型TD结合两阶段法最具桥梁潜力。
conclusion: 识别问题与维度灾难是双方共同挑战，交叉融合有望推动方法论实质性进步。
---

## 摘要
本文综述了两个深度关联、但源自不同学科传统并处理同一基本问题的文献：结构计量经济学中的动态离散选择（DDC）与机器学习中的逆强化学习（IRL）。两者都旨在从观察到的序列行为中推断决策者的偏好，并假设个体在形式化为马尔可夫决策过程（MDP）的动态不确定环境中行动，以最大化期望奖励函数。尽管起源独立，这两个领域已汇聚到相似的数学表述上。我们表明，当前在IRL中盛行的（软Q学习）框架与具有可加极值偏好冲击的DDC模型密切相关，二者产生相同的softmax（多项logit）选择概率和平滑贝尔曼方程，这些构成了经济学中结构估计的基础。我们比较了每个领域发展出的估计与计算方法。DDC侧重于最大似然估计、条件选择概率估计量以及策略迭代方法。IRL则发展出了可扩展的替代方法，包括最大熵方法、对抗式方法以及无模型时序差分估计量，这些方法借助深度神经网络可扩展到高维状态空间。将时序差分学习与计量经济学中经典的两步法相结合的无模型IRL估计量，代表了连接这两个文献的一个有前景的方向。两个领域都面临共同的基础性挑战：识别问题，即多个奖励函数都可以合理解释相同的观测行为；以及在求解底层MDP时的维度灾难。我们相信，跨领域的相互借鉴为两个领域的方法论进步提供了大量机会。

## Abstract
This article surveys two deeply connected literatures that approach the same fundamental problem from different disciplinary traditions: dynamic discrete choice (DDC) in structural econometrics and inverse reinforcement learning (IRL) in machine learning. Both seek to infer the preferences of decision makers from observed sequential behavior, assuming that individuals act to maximize an expected reward function within a dynamic, uncertain environment formalized as a Markov decision process (MDP).   Despite independent origins, the two fields have converged on similar mathematical formulations. We show that the (soft Q-learning) framework now prevalent in IRL is closely related to DDC models under additive extreme value preference shocks, yielding the same softmax (multinomial logit) choice probabilities and smooth Bellman equations that underpin structural estimation in economics. We compare the estimation and computational methods developed in each field. DDC has emphasized maximum likelihood estimation, conditional choice probability estimators, and policy iteration methods. IRL has developed scalable alternatives, including maximum entropy methods, adversarial approaches, and model-free temporal difference estimators that extend to high-dimensional state spaces using deep neural networks. Model-free IRL estimators that combine temporal difference learning with classical two-step methods from econometrics represent a promising direction for bridging the two literatures. Both fields confront shared foundational challenges: the identification problem, whereby multiple reward functions can rationalize the same observed behavior, and the curse of dimensionality in solving the underlying MDP. We believe that cross-fertilization offers substantial opportunities for methodological progress in both fields.

---

## 论文详细总结（自动生成）

# 论文总结：动态离散选择与逆强化学习——从人类行为推断偏好与信念

## 1. 论文的核心问题与整体含义

- **研究问题**：如何从人类或专家的序贯行为中推断其偏好与信念。动态离散选择（DDC）来自结构计量经济学，逆强化学习（IRL）来自机器学习，二者独立发展但解决同一根本问题：假设决策者在 MDP 环境中最大化期望奖励，从观察到的选择中反推奖励函数。
- **核心联系**：论文论证 IRL 中的“软 Q 学习”框架与 DDC 中带可加极值冲击的模型数学上高度同构，都产生 softmax/multinomial logit 选择概率与平滑 Bellman 方程。
- **动机差异**：DDC 强调反事实政策评估与福利分析；IRL 强调从专家演示中训练智能体、实现复杂任务自动化。
- **共同挑战**：识别问题（多个奖励函数可以解释同一行为）与维度灾难（求解底层 MDP 的计算复杂度随状态维度指数增长）。

## 2. 论文提出的方法论

- **统一 MDP 框架**
  - 定义状态空间 \(S\)、动作空间 \(A\)、奖励函数 \(r(s,a)\)、转移密度 \(p(s'|s,a)\) 与折现因子 \(\beta\)。
  - Bellman 最优方程：\(V(s)=\max_a [r(s,a)+\beta \sum_{s'}V(s')p(s'|s,a)]\)。
  - 求解方法：连续逼近、策略迭代、Q-learning、软 Q-learning、函数逼近与深度 Q 网络（DQN）。

- **DDC 侧方法**
  - 引入观察者不可见的极值（EV）偏好冲击，得到分析性条件选择概率（CCP）：
    \[
    \pi(a|s)=\frac{\exp\{Q(s,a)/\sigma\}}{\sum_{a'\in A(s)}\exp\{Q(s,a')/\sigma\}}
    \]
  - 估计方法：嵌套不动点（NFXP）、MPEC、条件选择概率（CCP/Hotz-Miller）估计量、嵌套伪似然（NPL/AM）、贝叶斯 MCMC、模拟矩（MSM）、间接推断、矩不等式、对抗式估计。
  - 高维状态空间：用神经网络等函数逼近 Bellman 残差；SEES 惩罚似然、NNES、均衡传播求梯度。

- **IRL 侧方法**
  - 最大边际方法：要求专家累积奖励超过备选策略。
  - Bayesian IRL：将奖励函数视为随机变量，用 MCMC 从后验采样。
  - 最大因果熵 IRL（MCE）：在特征匹配约束下最大化策略因果熵，导出的 softmax 策略与 DDC 相同。
  - Guided Cost Learning（GCL）：用神经网络、重要性采样与惰性策略优化将最大熵 IRL 扩展到连续控制。
  - 对抗式方法：GAIL 通过判别器-生成器博弈匹配占用测度；AIRL 用结构化判别器 \(f_\phi(s,a,s')=g_\phi(s,a)+\beta h_\phi(s')-h_\phi(s)\) 分离内在奖励与塑造势，实现可迁移奖励。
  - 无模型 IRL：Adusumilli–Eckardt 的 TD/CCP 估计量、GLADIUS 估计量，用原始转移数据 \((s,a,s')\) 避免显式估计 \(p(s'|s,a)\)。

- **识别与正则化**
  - Hotz-Miller 反演只能识别 Q 值差；潜在奖励塑造 \(r_h(s,a)=r(s,a)+\beta \sum_{s'}h(s')p(s'|s,a)-h(s)\) 保持策略不变。
  - 常用识别约束：参数化奖励、锚定/归一化动作、理性预期、已知 \(\beta\) 或多环境观测。

## 3. 实验设计

- 本文是综述，**没有自有的新实验或基准测试**。
- 它系统比较了 DDC 与 IRL 的方法族、数学表述和估计策略，而不是在固定数据集上跑算法对比。
- 文中引用的应用场景作为例证，包括：
  - Google Maps 路径偏好推断（360M 参数、110M 行程）。
  - Atari 游戏、国际象棋/围棋/将棋的 DQN 与 AlphaZero。
  - 出租车路径选择、自动驾驶轨迹预测与驾驶行为学习。
  - 连续内容消费（小说/电视剧）中的消费者保留与定价反事实。
  - 丹麦汽车税改革等 DDC 结构性反事实研究。
- 因此，“实验设计”更准确地说是**文献组织与方法对比框架**：按 DDC 估计方法、IRL 方法（最大熵、对抗、无模型）、识别问题、维度灾难等主题展开。

## 4. 资源与算力

- 本综述**未报告自身的计算资源或算力消耗**（如 GPU 型号、数量、训练时长）。
- 文中提到大规模应用所需的大算力，但仅为间接说明：
  - Google Maps IRL 应用：360M 参数、110M 次出行。
  - DQN/AlphaZero 等方法依赖大量模拟和自对弈训练。
  - 深度 RL 通常需要大量离线模拟迭代。
- 因此，资源与算力部分在原论文中**没有明确、可总结的量化信息**。

## 5. 实验数量与充分性

- 作为综述，**不涉及实验数量**，无法用“几组实验/消融”来评价。
- 但可以评价其文献覆盖的充分性：
  - 覆盖了 DDC 的经典估计量（NFXP、CCP、NPL、MCMC）和 IRL 的主要方法（最大熵、对抗、无模型 TD）。
  - 覆盖了识别问题、维度灾难、函数逼近、DQN 收敛性等理论议题。
  - 对比相对客观：既指出两领域数学同构，也指出目标、随机性解释与计算策略的差异。
- 潜在不足：作为综述，方法选择有作者侧重，不可能穷尽所有最新工作；对某些方法的收敛性、有限样本性质讨论偏简述。

## 6. 主要结论与发现

- DDC 与 IRL 本质上是同一问题的两个学术传统，数学表述已经收敛：软 Q-learning = 带 EV 冲击的 DDC。
- 两者都面临相同的双级优化结构：外层选择奖励函数以最大化数据拟合，内层求解 MDP 得到最优策略。
- 两领域方法互补：
  - DDC 发展出最大似然、CCP、NPL 等严格统计推断。
  - IRL 发展出最大熵、对抗式、无模型 TD 等可扩展到高维状态空间的方法。
- 结合 TD 学习与计量经济学两步法的无模型 IRL 估计量，是连接两个文献的最有希望方向。
- 识别问题无法根本回避，必须施加强先验假设才能唯一反推奖励与信念；错误的识别假设会损害反事实预测与跨环境迁移。
- 维度灾难仍然存在，神经网络和随机化只是缓解而非证明“打破”了灾难。
- 深度 RL 的成功同时挑战了完全理性假设，提示需要研究有界理性下的 DDC/IRL 推断。

## 7. 优点

- **跨学科桥梁**：清晰建立 DDC 与 IRL 的数学等价关系，对两个领域都有启发价值。
- **系统全面**：从 MDP 基础、Bellman 算子、EV 冲击、CCP 反演到 DQN、GAIL/AIRL、无模型 TD 估计量，覆盖完整。
- **抓住关键矛盾**：识别问题与维度灾难被作为贯穿主线，而非简单罗列算法。
- **强调无模型 TD 与两步估计的融合**：给出具体、有前景的研究方向。
- **客观对比**：既指出共性，也指出 DDC 与 IRL 在随机性解释、目标和计算方法上的深层差异。
- **兼顾理论与应用**：从 Google Maps、自动驾驶到连续内容消费，展示方法在真实世界中的价值。

## 8. 不足与局限

- **综述性质，无新实验**：没有提供原始数据集上的数值对比，无法直接验证各方法优劣。
- **识别问题仍依赖强假设**：虽然讨论了部分识别，但未给出普遍可用的弱假设识别策略。
- **模型-free 并非假设-free**：无模型 IRL 仍隐含理性预期等强假设，文中虽指出但未提出完整解决方案。
- **深度方法的理论保证有限**：对 DQN、AIRL 等在高维非凸优化下的收敛性、有限样本性质讨论仍较初步。
- **跨环境迁移困难**：GAIL/模型-free 估计量在转移动态变化时可能失效，依赖环境覆盖与人造奖励分解。
- **人类理性假设受挑战**：AlphaZero 超越人类提示人类可能是有界理性的，但文中对 DDC/IRL 如何适应有界理性的讨论相对薄弱。
- **选择性偏误风险**：综述覆盖了作者认为重要的方法，但对于刚出现或非主流的方法可能覆盖不足，例如部分最新的 offline RL 或行为经济学约束方法讨论较简略。

（完）
